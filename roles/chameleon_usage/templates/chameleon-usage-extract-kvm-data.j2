#! /bin/sh

docker run --rm --net=host \
    -v "{{ chameleon_usage_config_dir }}/my.cnf:/etc/mysql/my.cnf" \
    {{ chameleon_usage_docker_image }} \
    extract_kvm_data --mysql-conf /etc/mysql/my.cnf 2>&1
